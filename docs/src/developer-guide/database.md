# Database & Models

## ORM

Pretorin CRM uses SQLAlchemy 2.0 with the declarative base pattern. All models are defined in `backend/app/models/models.py`.

## Connection

`backend/app/database.py` configures the engine and session:

- Reads `DATABASE_URL` from environment.
- Supports PostgreSQL (`postgresql://`) and SQLite (`sqlite:///`).
- SQLite uses `check_same_thread=False` for multi-threaded access.
- Connection pooling for PostgreSQL: `pool_size=10`, `max_overflow=20`, `pool_pre_ping=True`, `pool_recycle=3600`.
- `SessionLocal` provides scoped database sessions.
- `get_db()` dependency yields a session per request and closes it after.

## Entity Relationship Diagram

```
User ──────────────────┐
 │                     │
 ├── Contact ──────── Communication
 │     │                    │
 │     └── Contract ◄──────┘ (via contract_contacts)
 │
 ├── Opportunity ─────┬── Teaming ──── Account
 │     │              ├── Proposal
 │     │              ├── OpportunityEvent
 │     │              ├── CaptureNote
 │     │              ├── Attachment
 │     │              └── ContractVehicle (via opportunity_vehicles)
 │
 ├── GmailIntegration
 ├── Compliance
 └── AuditLog
```

## Models

### User

Core user model for authentication and authorization.

| Column | Type | Notes |
|--------|------|-------|
| `id` | String(36) | UUID primary key |
| `email` | String(255) | Unique, indexed |
| `name` | String(150) | Display name |
| `hashed_password` | String(255) | bcrypt hash |
| `role` | String(20) | "admin" or "user" |
| `is_active` | Boolean | Account enabled/disabled |
| `auth_provider` | String(20) | "local" or "google" (nullable) |
| `google_id` | String(255) | Google sub claim (unique, nullable) |
| `api_key_hash` | String(255) | HMAC-SHA256 hash of API key |
| `api_key_prefix` | String(20) | Display prefix (e.g., "crm_a1b2...") |
| `password_reset_token` | String(255) | Hashed reset token |
| `password_reset_expires` | DateTime | Token expiry |

### Account

Organizations in the GovCon ecosystem.

| Column | Type | Notes |
|--------|------|-------|
| `id` | String(36) | UUID |
| `name` | String(300) | Organization name |
| `account_type` | String(30) | government_agency, prime_contractor, etc. |
| `parent_agency` | String(300) | Parent organization |
| `office` | String(300) | Office/division |
| `location` | String(300) | Physical location |
| `website` | String(2048) | URL |
| `created_by_user_id` | FK(users.id) | Creator for authorization |

### Contact

People associated with accounts.

| Column | Type | Notes |
|--------|------|-------|
| `id` | String(36) | UUID |
| `first_name` | String(100) | |
| `last_name` | String(100) | |
| `email` | String(255) | Indexed, used for Gmail matching |
| `phone` | String(50) | |
| `organization` | String(200) | |
| `contact_type` | String(20) | individual, commercial, government |
| `status` | String(20) | cold, warm, hot |
| `needs_follow_up` | Boolean | |
| `follow_up_date` | DateTime | Indexed for follow-up queries |
| `assigned_user_id` | FK(users.id) | Responsible CRM user |
| `account_id` | FK(accounts.id) | Linked account |

### Opportunity

Full capture pipeline entity with soft delete support.

| Column | Type | Notes |
|--------|------|-------|
| `id` | String(36) | UUID |
| `title` | String(300) | |
| `is_government_contract` | Boolean | |
| `stage` | String(20) | 8 pipeline stages, indexed |
| `estimated_value` | Float | Dollar value |
| `naics_code` | String(20) | |
| `set_aside_type` | String(30) | SB, 8a, HUBZone, etc. |
| `capture_manager_id` | FK(users.id) | |
| `deleted_at` | DateTime | Soft delete timestamp |

### Many-to-Many Tables

- `contract_contacts` -- Links contracts to contacts.
- `opportunity_vehicles` -- Links opportunities to contract vehicles.

## Conventions

- **All IDs** are UUID strings generated by `utils.generate_id()`.
- **Timestamps** use `datetime.now(timezone.utc)` for UTC consistency.
- **Soft deletes** use a nullable `deleted_at` column (currently only on Opportunity).
- **Cascade deletes** are configured on relationships where child records should be removed with parents.
- **Indexes** are added on frequently queried columns (email, status, stage, deleted_at, foreign keys).
- **CHECK constraints** enforce valid enum values at the database level (account_type, contact_type, status, stage, set_aside_type, etc.).

## Migrations

The project uses **Alembic** for database migrations. Migrations are stored in `backend/alembic/versions/` and run automatically before the application starts via `entrypoint.sh`:

```bash
alembic upgrade head
```

Current migrations:

1. `c769fd9e5252_initial_schema.py` -- Full initial schema creation.
2. `a2b3c4d5e6f7_add_index_and_check_constraints.py` -- Adds indexes and CHECK constraints.

When adding new columns or modifying the schema, generate a new migration:

```bash
cd backend
alembic revision --autogenerate -m "description of change"
```
