# Backend API URL
VITE_API_BASE_URL=http://localhost:8000

# Google Sign-In - Optional
# Use the same Client ID from your Google OAuth2 credentials
# (same value as GOOGLE_CLIENT_ID in backend/.env)
VITE_GOOGLE_CLIENT_ID=
